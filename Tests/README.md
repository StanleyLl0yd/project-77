# Tests

Automated tests, playtest support and validation fixtures for Project 77 live here.

Priority during Prototype 0.1:

- deterministic core-puzzle rules;
- level/config validation;
- basic smoke tests;
- analytics contract checks where practical;
- representative playtest fixtures.

Save migrations, economy formulas, purchase idempotency and Remote Config parsing become mandatory regression areas as those systems enter scope.
