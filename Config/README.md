# Config

Schemas, safe defaults and data-driven game configuration will live here as systems enter scope.

Configuration must be versioned and validated. Production Remote Config overrides must always have known-good in-build defaults and rollback paths.

During Prototype 0.1 keep this minimal: only configuration needed to define/test puzzle variants, levels, rewards and prototype telemetry.
